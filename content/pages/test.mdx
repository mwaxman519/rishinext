---
title: Test MDX Page
description: A test page to verify MDX implementation and demonstrate TinaCMS schema capabilities
date: 2025-02-02
---

# Test Page with TinaCMS Components

This page demonstrates our TinaCMS schema capabilities with various components and MDX features:

<callout type="info" text="This page showcases different callout types and MDX features!" />

## Rich Text Features

Here's some **bold text** and *italic text*.

## Component Examples

Let's test our callout component with different types:

<callout type="warning" text="Be careful when editing content directly!" />

<callout type="success" text="The schema configuration is working properly!" />

<callout type="error" text="This demonstrates an error callout style" />

## Code Examples

```typescript
// Example of TypeScript code
function createCallout(type: 'info' | 'warning' | 'success' | 'error', text: string) {
  return <callout type={type} text={text} />;
}
```

## Next Steps

<callout type="info" text="Try editing this page using the TinaCMS interface to see how the schema works in practice." />