Update rsync Sync Script for Bi-Directional Sync
Tell the Replit Agent to modify the sync script as follows:

bash
Copy
Edit
#!/bin/bash

# Ensure we are on the correct branch
git checkout staging

# Fetch the latest changes from static branch
git fetch origin static
git checkout static
git pull origin static

echo "🔄 Syncing MDX content between staging and static branches..."

# **Step 1: Push new pages from staging → static**
rsync -av --ignore-existing --delete ../static/content/ ./content/

# **Step 2: Pull new edits from static → staging**
rsync -av --ignore-existing ./content/ ../static/content/

# **Commit the updated content back to static**
git add content/
git commit -m "Sync MDX content between staging and static"
git push origin static

# Switch back to staging
git checkout staging

echo "✅ MDX content sync completed successfully."
✅ 2. Modify GitHub Actions Workflow to Sync Both Ways
📌 Modify .github/workflows/build.yml in staging to run the bi-directional sync before each build:

yaml
Copy
Edit
name: Deploy Static Site

on:
  push:
    branches:
      - staging  # Runs when `staging` is updated

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          ref: staging

      - name: Install Dependencies
        run: npm install

      - name: Sync MDX Content Bi-Directionally
        run: |
          git fetch origin static
          git checkout static
          git pull origin static
          rsync -av --ignore-existing --delete ../static/content/ ./content/
          rsync -av --ignore-existing ./content/ ../static/content/
          git add content/
          git commit -m "Sync MDX content between staging and static"
          git push origin static
          git checkout staging

      - name: Build Static Site
        run: npm run build

      - name: Push Static Build to Static Branch
        run: |
          git checkout static
          git pull origin static
          git add out/
          git commit -m "Auto-update static site build"
          git push origin static
🔥 What This Setup Does
Action	Source	Destination	Purpose
New MDX pages from staging → static	staging/static/content/	static/content/	If you generate MDX files via script, they sync to static.
Edits from static (TinaCMS) → staging	static/content/	staging/static/content/	If TinaCMS edits content, it gets pulled back into staging.
Static site is built (staging → static/out/)	staging	static/out/	Full site build is pushed for deployment.
✅ Final Steps
✔ Tell the Replit Agent: "Proceed with setting up the bi-directional rsync sync process."
✔ Ensure static/content/ contains pages generated by the agent.
✔ Test: Add pages via script in staging, verify they appear in TinaCMS UI.
✔ Make an edit in TinaCMS, verify it syncs back to staging.

